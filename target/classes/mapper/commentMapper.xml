<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.CommentDAO">

    <!-- 댓글 작성 -->
    <insert id="insertComment" parameterType="dto.CommentDTO">
        INSERT INTO post_comment (boardId, userId, parentComment, content, createTime)
        VALUES (#{boardId}, #{userId}, #{parentComment}, #{content}, NOW())
    </insert>
    
    <!-- 대댓글  -->
    <insert id="insertReply" parameterType="dto.CommentDTO">
	    INSERT INTO post_comment (boardId, userId, parentComment, content, createTime)
	    VALUES (#{boardId}, #{userId}, #{parentComment}, #{content}, NOW())
	</insert>

    <!-- 댓글 목록 조회 -->
    <select id="getCommentList" parameterType="int" resultType="dto.CommentDTO">
        SELECT * FROM post_comment WHERE boardId = #{boardId} ORDER BY createTime ASC
    </select>
    
    <!-- 대댓글을 가져오는 쿼리 -->
	<select id="getRepliesForComment" parameterType="int" resultType="dto.CommentDTO">
	    SELECT * FROM post_comment WHERE parentComment = #{commentId} ORDER BY createTime ASC
	</select>

    <!-- 댓글 수정 -->
    <update id="updateComment" parameterType="dto.CommentDTO">
        UPDATE post_comment SET content = #{content} WHERE commentId = #{commentId}
    </update>

    <!-- 댓글 삭제 -->
    <delete id="deleteComment" parameterType="int">
        DELETE FROM post_comment WHERE commentId = #{commentId}
    </delete>

</mapper>
